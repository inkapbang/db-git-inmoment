SET QUOTED_IDENTIFIER ON 
GO
SET ANSI_NULLS ON 
GO
CREATE PROCEDURE [dbo].[sp_SS]
AS
PRINT
'
SELECT
		--*
		--TOP 25 *
		--REPLACE(CONVERT(varchar(20), (CAST(    		COUNT(1)    	AS money)), 1), ''.00'', '''')
		
		
		
FROM
		
		
		
		
WHERE
		
		

--GROUP BY
		
		
		
--ORDER BY
		
								
						
'
GO
SET QUOTED_IDENTIFIER OFF 
GO
SET ANSI_NULLS OFF 
GO

GO
